import type { SchemaTypeDefinition } from "sanity";
import { masthead } from "./masthead";
import { testimonial } from "./testimonial";

export const schemaTypes: SchemaTypeDefinition[] = [masthead, testimonial];
