/**
 * Single source for masthead ISR interval (seconds).
 * Next.js `export const revalidate` in `page.tsx` must be this literal value for static analysis.
 */
export const MASTHEAD_REVALIDATE_SECONDS = 60;

export const MASTHEAD_CACHE_TAG = "sanity:masthead";
